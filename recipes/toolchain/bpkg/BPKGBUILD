pkgscope="bingux"
pkgname="bpkg"
pkgver="0.1.0"
pkgarch="x86_64-linux"
pkgdesc="Bingux user package manager"
license="GPL-3.0-or-later"
depends=()
exports=("bin/bpkg")
source=()
sha256sums=()
package() {
    mkdir -p "$PKGDIR/bin"
    cp "/home/kieran/dev/bingux/target/release/bpkg" "$PKGDIR/bin/bpkg"
    chmod +x "$PKGDIR/bin/bpkg"
}
